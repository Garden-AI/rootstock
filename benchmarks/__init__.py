"""Benchmarks for Rootstock.

``ipc_overhead`` -- measure the i-PI IPC cost of ``RootstockCalculator`` by
comparing it against the same calculator called directly inside its pre-built
env. Run with ``python -m benchmarks.ipc_overhead --help``.
"""
